<section class="section">
  <div class="page-width page-width--narrow">
    <h1 class="display" style="font-size:clamp(2rem,4vw,3rem);">{{ page.title }}</h1>
    <div class="rte" style="margin-top:1.5rem;">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page"
}
{% endschema %}
